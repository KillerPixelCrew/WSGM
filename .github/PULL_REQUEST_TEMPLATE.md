<!-- Thanks for contributing to WSGM! A few short sections help review go fast. -->

## What & why

<!-- What does this PR change, and what problem does it solve?
     Link related issues: Fixes #123 -->

## How it was tested

<!-- WSGM's core runs as the game-mode session on real hardware — "compiles" is not "works".
     Tell us what you actually ran: -->

- [ ] Built locally with `build.ps1`
- [ ] Tested in **game mode on real hardware** (which device? e.g. MSI Claw, ROG Ally, desktop)
- [ ] Desktop-only testing (`--settings` / `--overlay-test`) — say so, that's fine for UI-only changes

## Touches Steam's CEF / injected scripts?

<!-- Anything under the Steam integration (injected JavaScript, webpack/React discovery,
     store patching) MUST be verified against a live Steam client — these mechanisms look
     refactorable but break invisibly until run inside Steam. If you changed them, describe
     how you verified against the running client. If not, delete this section. -->

## Screenshots

<!-- For UI changes: before/after of the overlay, taskbar, or Settings. Delete if not visual. -->

## Notes for the reviewer

<!-- Trade-offs, things you're unsure about, follow-ups you left out on purpose. -->
